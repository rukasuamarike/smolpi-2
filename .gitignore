pi-agent
pi-agent.smolmachine
*.tar
*.smol
node_modules/
bin/
.pi/APPEND_SYSTEM.md
# Large model weights — download locally, never commit (see README / `make doctor`)
models/
*.gguf
# Local runtime state
.pi/umi_profile.json
.pi/progress.md
# umi profile lives at repo root; commit example.umi_profile.json instead
.umi_profile.json
# Local host config (copy .env.example → .env); .env.example stays tracked
.env
.env.local
